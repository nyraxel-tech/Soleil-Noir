name: Scrape AnimeHeaven

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

concurrency:
  group: scrape
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install dependencies
        run: pip install requests beautifulsoup4 --quiet

      - name: Scrape AnimeHeaven
        run: |
          python3 << 'PYEOF'
          import requests
          from bs4 import BeautifulSoup
          import json, re
          from datetime import datetime, timezone

          headers = {
              "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/120.0.0.0 Safari/537.36",
              "Accept": "text/html,application/xhtml+xml",
              "Accept-Language": "en-US,en;q=0.9",
              "Referer": "https://animeheaven.me/",
          }

          r = requests.get("https://animeheaven.me/new.php", headers=headers, timeout=15)
          print(f"Status: {r.status_code}, Size: {len(r.text)} bytes")

          soup = BeautifulSoup(r.text, "html.parser")
          animes = []
          seen = set()
          skip = {"New","Popular","Fall","Random","MORE","POPULAR","Tools","Anime Heaven",""}

          for a_tag in soup.find_all("a", href=lambda h: h and "/anime.php?" in h):
              img = a_tag.find("img")
              if not img:
                  continue
              titre = img.get("alt", "").strip()
              if not titre or titre in skip:
                  continue
              if any(x in titre.lower() for x in ["logo","icon","burger","prev","next","sun","discord","twitter"]):
                  continue
              href = a_tag.get("href", "")
              ah_id = href.split("?")[-1]
              src = img.get("src", "")
              img_code = src.split("?")[-1] if "?" in src else ""
              if not ah_id or not img_code or ah_id in seen or len(ah_id) < 3:
                  continue
              seen.add(ah_id)
              parent = a_tag.parent
              text = parent.get_text(" ", strip=True) if parent else ""
              ep_match = re.search(r'\b(\d+)\b', text)
              ep = int(ep_match.group(1)) if ep_match else 1
              time_match = re.search(r'(\d+)\s*(h|d)\s*ago', text)
              ago = f"{time_match.group(1)}{time_match.group(2)}" if time_match else "1j"
              titre_jp = ""
              grandparent = parent.parent if parent else None
              if grandparent:
                  lines = [l.strip() for l in grandparent.get_text("\n", strip=True).split("\n") if l.strip()]
                  try:
                      idx = next(i for i, l in enumerate(lines) if titre in l)
                      if idx + 1 < len(lines):
                          c = lines[idx + 1]
                          if not re.search(r'\d+\s*(h|d)\s*ago', c) and c not in ["-", titre]:
                              titre_jp = c
                  except StopIteration:
                      pass
              animes.append({"titre": titre, "titreJp": titre_jp, "ep": ep, "ago": ago, "img": img_code, "ahId": ah_id})
              if len(animes) >= 80:
                  break

          print(f"Found {len(animes)} animes")
          if animes:
              print(f"First: {animes[0]['titre']}, Last: {animes[-1]['titre']}")

          with open("data.json", "w") as f:
              json.dump({"animes": animes, "updatedAt": datetime.now(timezone.utc).isoformat(), "source": "github-actions", "count": len(animes)}, f, ensure_ascii=False, indent=2)
          print("data.json written")
          PYEOF

      - name: Commit and push data.json
        run: |
          git config user.name "AniTrack Bot"
          git config user.email "bot@anitrack.app"
          git pull origin main
          git add data.json
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "🔄 Update anime data - $(date -u '+%Y-%m-%d %H:%M UTC')"
            git push
          fi
